rules_version = '2';
// Each signed-in user can read/write only their own ledger document.
service cloud.firestore {
  match /databases/{database}/documents {
    match /ledgers/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
